name: resnet18_attr

params:
  _target_: models.attr_factory.get_attr_model
  model_name: resnet18
  num_classes: 5
  pretrained: true

training:
  epochs: 50
  early_stopping: true
  save_path: "resnet18_attr_best_model.pt"
  device: cuda:0
  patience: 10
  min_delta: 0.0
