name: seresnet-50

params:
  _target_: timm.create_model
  pretrained: true
  model_name: seresnet50.a2_in1k
  num_classes: 7

training:
  epochs: 100
  early_stopping: true
  save_path: "seresnet50_best_model.pt"
  device: cuda:6
  patience: 10
  min_delta: 0.0 
  freeze_ratio: 0.0
