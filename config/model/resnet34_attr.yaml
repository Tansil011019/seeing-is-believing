name: resnet34_attr

params:
  _target_: models.attr_factory.get_attr_model
  model_name: resnet34
  num_classes: 5
  pretrained: true

training:
  epochs: 50
  early_stopping: true
  save_path: "resnet34_attr_best_model.pt"
  device: cuda:0
  patience: 10
  min_delta: 0.0
